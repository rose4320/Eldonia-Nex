# 多言語・多通貨対応 実装ガイド

## 📋 概要

Eldonia-Nex プラットフォームは、グローバル展開を見据えた多言語・多通貨対応を実装しています。

## 🌍 対応言語

| コード | 言語 | ネイティブ表記 |
|--------|------|----------------|
| ja | Japanese | 日本語 |
| en | English | English |
| ko | Korean | 한국어 |
| zh-CN | Chinese (Simplified) | 简体中文 |
| zh-TW | Chinese (Traditional) | 繁體中文 |

## 💰 対応通貨

| コード | 通貨名 | シンボル | 小数点桁数 |
|--------|--------|----------|------------|
| JPY | Japanese Yen | ¥ | 0 |
| USD | US Dollar | $ | 2 |
| EUR | Euro | € | 2 |
| KRW | South Korean Won | ₩ | 0 |
| CNY | Chinese Yuan | ¥ | 2 |
| TWD | New Taiwan Dollar | NT$ | 0 |

## 🔧 バックエンド実装

### モデル構成

#### Language モデル
```python
Language.objects.all()
# 対応言語の一覧を取得
```

#### Currency モデル
```python
Currency.objects.all()
# 対応通貨の一覧を取得
```

#### ExchangeRate モデル
```python
ExchangeRate.objects.filter(
    from_currency_id='JPY',
    to_currency_id='USD'
).order_by('-effective_date').first()
# 最新の為替レートを取得
```

### API エンドポイント

#### 1. 言語一覧取得
```bash
GET /api/v1/localization/languages/
```

**レスポンス例:**
```json
[
  {
    "code": "ja",
    "name": "Japanese",
    "native_name": "日本語",
    "is_active": true,
    "is_default": true,
    "sort_order": 1
  },
  {
    "code": "en",
    "name": "English",
    "native_name": "English",
    "is_active": true,
    "is_default": false,
    "sort_order": 2
  }
]
```

#### 2. 通貨一覧取得
```bash
GET /api/v1/localization/currencies/
```

**レスポンス例:**
```json
[
  {
    "code": "JPY",
    "name": "Japanese Yen",
    "symbol": "¥",
    "decimal_places": 0,
    "is_active": true,
    "is_default": true,
    "sort_order": 1
  }
]
```

#### 3. 為替レート取得
```bash
GET /api/v1/localization/currencies/rates/?base=JPY
```

**レスポンス例:**
```json
[
  {
    "from_currency_code": "JPY",
    "to_currency_code": "USD",
    "rate": "0.0067",
    "effective_date": "2025-12-13T16:00:00Z",
    "source": "manual"
  }
]
```

#### 4. 通貨換算
```bash
POST /api/v1/localization/currencies/convert/
Content-Type: application/json

{
  "amount": 10000,
  "from_currency": "JPY",
  "to_currency": "USD"
}
```

**レスポンス例:**
```json
{
  "amount": "10000",
  "from_currency": "JPY",
  "to_currency": "USD",
  "converted_amount": "67.00",
  "rate": "0.0067",
  "rate_date": "2025-12-13T16:00:00Z"
}
```

#### 5. ユーザー地域設定取得
```bash
GET /api/v1/localization/users/me/locale/
Authorization: Token <auth_token>
```

**レスポンス例:**
```json
{
  "preferred_language": "ja",
  "preferred_currency": "JPY",
  "timezone": "Asia/Tokyo"
}
```

#### 6. 言語設定更新
```bash
PUT /api/v1/localization/users/me/language/
Authorization: Token <auth_token>
Content-Type: application/json

{
  "language": "en"
}
```

#### 7. 通貨設定更新
```bash
PUT /api/v1/localization/users/me/currency/
Authorization: Token <auth_token>
Content-Type: application/json

{
  "currency": "USD"
}
```

## 💻 フロントエンド実装

### 1. LanguageSwitcher コンポーネント

ヘッダーやナビゲーションに配置する言語切り替えボタン。

```tsx
import LanguageSwitcher from '@/components/localization/LanguageSwitcher';

export default function Header() {
  return (
    <header>
      {/* ... */}
      <LanguageSwitcher />
    </header>
  );
}
```

**特徴:**
- 国旗アイコン表示
- ドロップダウンメニュー
- 選択した言語でページをリロード

### 2. LocalizationSettings コンポーネント

設定画面で使用する言語・通貨変更フォーム。

```tsx
import LocalizationSettings from '@/components/localization/LocalizationSettings';

export default function SettingsPage() {
  return (
    <div>
      <LocalizationSettings />
    </div>
  );
}
```

**特徴:**
- 言語セレクトボックス
- 通貨セレクトボックス
- APIと連携して自動保存

### 3. CurrencyDisplay コンポーネント

金額を表示する際に、ユーザーの設定通貨に自動換算。

```tsx
import CurrencyDisplay from '@/components/localization/CurrencyDisplay';

export default function ProductCard({ price }: { price: number }) {
  return (
    <div>
      <CurrencyDisplay amount={price} fromCurrency="JPY" />
    </div>
  );
}
```

**特徴:**
- 自動通貨換算
- リアルタイム為替レート使用
- 小数点桁数自動調整

### 4. 翻訳テキスト使用

next-intl の `useTranslations` フックを使用。

```tsx
'use client';

import { useTranslations } from 'next-intl';

export default function MyComponent() {
  const t = useTranslations('common');
  
  return (
    <div>
      <h1>{t('welcome')}</h1>
      <button>{t('save')}</button>
    </div>
  );
}
```

## 🗂️ メッセージファイル管理

### ディレクトリ構造
```
frontend/
├── messages/
│   ├── ja.json       # 日本語
│   ├── en.json       # 英語
│   ├── ko.json       # 韓国語
│   ├── zh-CN.json    # 簡体中文（今後追加）
│   └── zh-TW.json    # 繁體中文（今後追加）
└── i18n.ts           # next-intl設定
```

### メッセージファイル例 (ja.json)
```json
{
  "common": {
    "appName": "Eldonia-Nex",
    "welcome": "ようこそ",
    "save": "保存",
    "cancel": "キャンセル"
  },
  "nav": {
    "home": "ホーム",
    "gallery": "ギャラリー"
  }
}
```

## 🚀 初期データ投入

### コマンド実行
```bash
cd backend
python manage.py load_localization_data
```

**投入内容:**
- 5言語のマスタデータ
- 6通貨のマスタデータ
- JPYを基準とした為替レート
- 逆レート（自動計算）

## 📝 使用例

### ユーザーストーリー

#### 1. 韓国のユーザーが初めて訪問
```
1. サイトにアクセス（デフォルト: 日本語）
2. LanguageSwitcher で "한국어" を選択
3. 全てのUI表記が韓国語に切り替わる
4. 価格表示が自動的に ₩ で表示される
```

#### 2. アメリカのユーザーが商品を購入
```
1. 言語を English に設定
2. 通貨を USD に設定
3. 商品価格が自動的に $ で換算表示
4. ¥10,000 → $67.00 と表示される
```

## 🔄 今後の拡張計画

### Phase 2
- [ ] 機械翻訳API統合（Google Translate / DeepL）
- [ ] コンテンツ自動翻訳機能
- [ ] ユーザー投稿の多言語対応

### Phase 3
- [ ] 為替レート自動更新（外部API連携）
- [ ] 地域別価格設定機能
- [ ] タイムゾーン対応の日時表示

### Phase 4
- [ ] 仮想通貨対応（BTC, ETH等）
- [ ] 地域限定コンテンツ配信
- [ ] 多言語SEO最適化

## 🐛 トラブルシューティング

### 言語切り替えができない
```
- ブラウザのキャッシュをクリア
- ローカルストレージの authToken を確認
- API応答を確認（ネットワークタブ）
```

### 通貨換算が正しくない
```
- 為替レートが最新か確認
- ExchangeRateモデルのデータを確認
- APIレスポンスの rate フィールドを確認
```

### 翻訳テキストが表示されない
```
- messages/{locale}.json ファイルが存在するか確認
- JSON構文エラーがないか確認
- i18n.ts の設定を確認
```

## 📚 参考リンク

- [next-intl ドキュメント](https://next-intl-docs.vercel.app/)
- [ISO 639-1 言語コード](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes)
- [ISO 4217 通貨コード](https://en.wikipedia.org/wiki/ISO_4217)
- [Django 多言語対応](https://docs.djangoproject.com/en/5.1/topics/i18n/)

---

**更新日**: 2025-12-13
**バージョン**: 0.4.0
