# Eldonia-Nex 開発環境ルール設定書

*開発チーム全体での一貫した開発環境構築・運用ガイドライン*

---

## 📋 ドキュメント情報

**ドキュメント名**: 開発環境ルール設定書  
**バージョン**: 1.0  
**作成日**: 2025年11月9日  
**管理責任者**: 開発チーム  
**準拠文書**: [統合要件定義書](./00_統合要件定義書.md)

---

## 🎯 目的・方針

### 目的

- 開発チーム全体で統一された開発環境の構築と運用
- 技術的負債の削減と開発効率の向上
- 新メンバーのオンボーディング時間短縮
- 本番環境との一貫性確保

### 基本方針

1. **一貫性**: 全開発者が同一の環境・手順で作業
2. **自動化**: 手動設定を最小化し、スクリプト化
3. **文書化**: すべての設定・手順を文書で管理
4. **標準準拠**: 業界標準とベストプラクティスの採用

---

## 🏗️ プロジェクト構成ルール

### ディレクトリ構造（必須）
```

eldinia-nex/
├── frontend/                 # Next.js SSRアプリケーション（ポート3000）
│   ├── app/                 # App Router構成
│   ├── components/          # 再利用可能コンポーネント
│   ├── lib/                 # ユーティリティ・設定
│   ├── public/              # 静的ファイル
│   ├── styles/              # スタイリングファイル
│   ├── package.json         # Next.js依存関係管理
│   ├── next.config.ts       # Next.js設定
│   ├── tailwind.config.ts   # TailwindCSS設定
│   └── tsconfig.json        # TypeScript設定
├── backend/                 # Django REST APIアプリケーション（ポート8000）
│   ├── eldinia_nex/         # プロジェクト設定
│   ├── users/               # ユーザー管理アプリ
│   ├── content/             # 作品管理アプリ
│   ├── marketplace/         # 決済・販売アプリ
│   ├── collaboration/       # コラボレーションアプリ
│   ├── events/              # イベント管理アプリ
│   ├── streaming/           # Live配信アプリ
│   ├── jobs/                # 仕事・マッチングアプリ
│   ├── gamification/        # ゲーミフィケーションアプリ
│   ├── manage.py            # Django管理コマンド
│   ├── requirements.txt     # Python依存関係
│   ├── db.sqlite3          # 開発用SQLiteデータベース
│   ├── static/             # 静的ファイル
│   ├── media/              # アップロードファイル
│   └── logs/               # ログファイル
├── docker/                  # Docker構成ファイル
├── scripts/                 # 自動化スクリプト
├── docs/                    # ドキュメント管理
├── .vscode/                 # VS Code設定
├── .env                     # 環境変数（ローカル設定）
├── .env.example             # 環境変数テンプレート
├── docker-compose.dev.yml   # 開発環境Docker構成
├── docker-compose.yml       # 本番環境Docker構成
└── README.md                # プロジェクト概要
```

---

## 🖥️ 開発サーバー実行ルール

### 【必須】ディレクトリ別実行規則

#### 1. Django Backend（必須: backendフォルダで実行）
```bash
# ❌ 間違った実行方法

cd eldinia-nex/
python manage.py runserver  # エラー: manage.pyが見つからない

# ✅ 正しい実行方法

cd eldinia-nex/backend/
python manage.py runserver 0.0.0.0:8000

# PowerShell環境での実行例

Set-Location c:\eldinia-nex\backend
python manage.py runserver 0.0.0.0:8000 --noreload
```

#### 2. Next.js Frontend（必須: frontendフォルダで実行）
```bash
# ❌ 間違った実行方法

cd eldinia-nex/
npm run dev  # エラー: package.jsonが見つからない

# ✅ 正しい実行方法

cd eldinia-nex/frontend/
npm run dev

# PowerShell環境での実行例

Set-Location c:\eldinia-nex\frontend
npm run dev
```

#### 3. 同時実行手順（開発時必須）
```bash
# ターミナル1: Djangoバックエンド起動

cd eldinia-nex/backend/
python manage.py runserver 0.0.0.0:8000

# ターミナル2: Next.jsフロントエンド起動

cd eldinia-nex/frontend/
npm run dev

# 確認URL
# - Frontend: http://localhost:3000
# - Backend API: http://localhost:8000/api/v1/
# - Django Admin: http://localhost:8000/admin/
```

---

## 🔧 技術スタック要件（統合要件定義書準拠）

### フロントエンド（frontend/）
```yaml
必須技術:
  - React: "18.3+"
  - Next.js: "15.0+" (App Router使用)
  - TypeScript: "5.7+"
  - TailwindCSS: "3.4+"
  
推奨ライブラリ:
  - Radix UI: UIコンポーネント
  - Framer Motion: アニメーション
  - Zod: バリデーション
  - React Hook Form: フォーム管理

コーディング規約:
  - ESLint + Prettier使用必須
  - TypeScript strict mode有効
  - コンポーネント名: PascalCase
  - ファイル名: kebab-case
```

### バックエンド（backend/）
```yaml
必須技術:
  - Python: "3.12+"
  - Django: "5.1+"
  - Django REST Framework: "3.15+"
  - PostgreSQL: "17+" (本番)
  - SQLite: 開発環境

必須パッケージ:
  - django-cors-headers: CORS対応
  - django-channels: WebSocket
  - celery: 非同期処理
  - redis: キャッシュ・セッション

コーディング規約:
  - PEP 8準拠
  - Black + isort使用
  - Type hints必須
  - Docstring必須（Google形式）
```

---

## 📁 VS Code 設定ルール

### 必須拡張機能
```json
{
  "recommendations": [
    "ms-python.python",
    "ms-python.black-formatter", 
    "bradlc.vscode-tailwindcss",
    "esbenp.prettier-vscode",
    "ms-vscode.vscode-typescript-next",
    "ms-toolsai.jupyter",
    "ms-azuretools.vscode-docker"
  ]
}
```

### ワークスペース設定
```json
{
  "python.defaultInterpreterPath": "./backend/venv/Scripts/python.exe",
  "python.terminal.activateEnvironment": true,
  "editor.formatOnSave": true,
  "editor.codeActionsOnSave": {
    "source.organizeImports": true
  },
  "typescript.preferences.importModuleSpecifier": "relative",
  "tailwindCSS.includeLanguages": {
    "typescript": "javascript",
    "typescriptreact": "javascript"
  },
  "files.associations": {
    "*.css": "tailwindcss"
  }
}
```

---

## 🛠️ 開発環境セットアップ手順

### 1. 初期セットアップ（新規開発者用）
```bash
# 1. リポジトリクローン

git clone https://github.com/organization/eldinia-nex.git
cd eldinia-nex

# 2. 環境変数設定

cp .env.example .env

# .envファイルを環境に応じて編集

# 3. バックエンドセットアップ

cd backend/
python -m venv venv

# Windows: .\venv\Scripts\activate
# Linux/Mac: source venv/bin/activate

pip install -r requirements.txt
python manage.py migrate
python manage.py createsuperuser

# 4. フロントエンドセットアップ

cd ../frontend/
npm install

# 5. 開発サーバー起動確認
# ターミナル1: cd backend/ && python manage.py runserver
# ターミナル2: cd frontend/ && npm run dev
```

### 2. 日次開発開始手順
```bash
# 1. 最新コード取得

git pull origin main

# 2. 依存関係更新確認

cd backend/ && pip install -r requirements.txt
cd ../frontend/ && npm install

# 3. データベースマイグレーション

cd backend/ && python manage.py migrate

# 4. 開発サーバー起動
# ターミナル1: cd backend/ && python manage.py runserver 0.0.0.0:8000
# ターミナル2: cd frontend/ && npm run dev
```

---

## 🐳 Docker環境ルール

### 開発環境（docker-compose.dev.yml）
```yaml
# 使用方法

docker-compose -f docker-compose.dev.yml up -d

# サービス構成

services:
  - frontend: Next.js (localhost:3000)
  - backend: Django (localhost:8000)  
  - db: PostgreSQL (localhost:5433)
  - redis: Redis (localhost:6379)
```

### 本番環境（docker-compose.yml）
```yaml
# 本番デプロイ用設定
# SSL証明書、環境変数、スケーリング設定含む
```

---

## 🌐 ポート管理ルール

### 開発環境ポート割り当て（固定）
```yaml
必須ポート:
  Frontend (Next.js): 3000
  Backend (Django): 8000
  PostgreSQL: 5433
  Redis: 6379

テスト用ポート:
  Frontend Test: 3001
  Backend Test: 8001
  
禁止ポート:
  - 80, 443: 本番環境用
  - 5432: PostgreSQL標準（本番用）
  - 3306: MySQL
```

---

## 📝 コーディング規約

### ファイル命名規則
```yaml
Frontend:
  - コンポーネント: PascalCase (UserProfile.tsx)
  - ページ: kebab-case (user-profile.tsx)
  - ユーティリティ: camelCase (formatUtils.ts)
  - スタイル: kebab-case (user-profile.module.css)

Backend:
  - モデル: PascalCase (UserProfile)
  - ファイル: snake_case (user_profile.py)
  - 関数: snake_case (get_user_profile)
  - 定数: UPPER_SNAKE_CASE (MAX_FILE_SIZE)
```

### Git規約
```yaml
ブランチ命名:
  - feature/機能名 (feature/user-authentication)
  - bugfix/バグ名 (bugfix/login-error)
  - hotfix/緊急修正 (hotfix/security-patch)

コミットメッセージ:
  - feat: 新機能追加
  - fix: バグ修正
  - docs: ドキュメント更新
  - style: コードスタイル修正
  - refactor: リファクタリング
  - test: テスト追加・修正
```

---

## � UIコンポーネント設計・開発ルール

### UI/UX設計書準拠必須事項

**UI/UX設計書（05_UI_UX設計書.md）の内容は権威ある仕様として必ず準拠すること**

### 基本レイアウト構成
```typescript
// 必須実装: 3層レイアウト構造
interface LayoutProps {
  children: ReactNode;
  pageTitle?: string;
  showHeader?: boolean;
  showFooter?: boolean;
  headerProps?: HeaderProps;
  footerProps?: FooterProps;
  className?: string;
}

// レイアウト構造（固定）
┌─────────────────────────────────────────┐
│ <Header {...headerProps} />             │ (高さ: 64px)
├─────────────────────────────────────────┤
│ <main className="min-h-screen">         │ (可変高)
│   {children}                            │
│ </main>                                 │
├─────────────────────────────────────────┤
│ <Footer {...footerProps} />             │ (最小高: 320px)
└─────────────────────────────────────────┘
```

### ヘッダーコンポーネント仕様（必須準拠）
```typescript
// 3カラムレイアウト: 30% | 40% | 30%
interface HeaderProps {
  brand: BrandSection;      // 左：ブランドセクション
  navigation: NavSection;   // 中央：ナビゲーション
  userSection: UserSection; // 右：ユーザーセクション
}

// ブランドセクション（PT Serif + グラデーション必須）
interface BrandSection {
  logo: {
    src: "/assets/logo/eldonia-nex-logo.png";
    alt: "Eldonia-Nex ファンタジー魔法陣ロゴ";
    width: 64; height: 64;
  };
  title: "Eldonia-Nex";
  subtitle: "クリエイタープラットフォーム";
  font: 'PT Serif'; // 必須：ブランド専用フォント
  gradient: 'linear-gradient(180deg, #FCD34D 0%, #F97316 100%)'; // 必須
}
```

### デザインシステム統一基準（必須準拠）
```css
/* 必須CSS変数 - UI/UX設計書準拠 */
:root {
  /* プライマリカラー */
  --primary: #6366F1;
  --primary-dark: #4F46E5;
  --primary-light: #A5B4FC;
  
  /* セカンダリカラー */
  --success: #10B981;
  --warning: #F59E0B;
  --error: #EF4444;
  --info: #3B82F6;
  
  /* ダークモード（デフォルトテーマ） */
  --bg-primary: #111827;
  --bg-secondary: #1F2937;
  --surface: #374151;
  --text-primary: #F9FAFB;
  --text-secondary: #D1D5DB;
  --border: #4B5563;
  
  /* ブランドグラデーション（必須） */
  --brand-gradient: linear-gradient(180deg, #FCD34D 0%, #F97316 100%);
  
  /* 固定サイジング */
  --header-height: 64px;
  --footer-min-height: 320px;
  --container-max-width: 1200px;
  --border-radius: 8px;
  --exp-bar-height: 6px;
}

/* ブランドテキスト専用スタイル（必須） */
.brand-title {
  font-family: 'PT Serif', serif;
  font-size: 32px;
  font-weight: 700;
  background: var(--brand-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin: 0;
  line-height: 1.2;
}

.brand-subtitle {
  font-family: 'PT Serif', serif;
  font-size: 24px;
  font-weight: 400;
  background: var(--brand-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin: 0;
  line-height: 1.2;
}
```

### タイポグラフィ階層（必須準拠）
```css
/* フォントファミリー階層 */
.brand-text {
  font-family: 'PT Serif', serif; /* ブランド専用 */
}
.primary-text {
  font-family: 'Noto Sans JP', 'Inter', sans-serif; /* 一般用 */
}
.code-text {
  font-family: 'JetBrains Mono', 'Fira Code', monospace; /* コード用 */
}

/* サイズ階層 */
.text-display { font-size: 48px; } /* Display */
.text-h1 { font-size: 32px; }      /* ブランドタイトル */
.text-h2 { font-size: 24px; }      /* サブタイトル */
.text-h3 { font-size: 20px; }      /* 見出し3 */
.text-body { font-size: 16px; }    /* 本文 */
.text-small { font-size: 14px; }   /* 小文字 */
.text-caption { font-size: 12px; } /* キャプション */
```

### レスポンシブ統一基準（必須準拠）
```css
/* 統一ブレークポイント */
@media (max-width: 767px) { 
  /* Mobile: ハンバーガーメニュー + 1列表示 */ 
}
@media (min-width: 768px) and (max-width: 1023px) { 
  /* Tablet: 折りたたみサイドバー + 2-3列表示 */ 
}
@media (min-width: 1024px) and (max-width: 1439px) { 
  /* Desktop: フル機能 + 4-5列表示 */ 
}
@media (min-width: 1440px) { 
  /* Large Desktop: 最大表示 + 6列表示 */ 
}
```

### 必須実装コンポーネント

1. **Layout**: 基本3層レイアウト
2. **Header**: ブランド + ナビゲーション + ユーザーセクション
3. **Footer**: 3カラム情報 + SNS + コピーライト
4. **Navigation**: レスポンシブナビゲーション
5. **UserSection**: 認証状態別UI（ログイン/未ログイン）
6. **ExperienceBar**: ゲーミフィケーション用プログレスバー

### アクセシビリティ必須対応（WCAG 2.1 AA準拠）
```html
<!-- ARIAラベル必須使用例 -->
<button aria-label="作品にいいねする" aria-pressed="false">
  <span aria-hidden="true">❤️</span>
</button>

<nav aria-label="メインナビゲーション">
  <ul role="menubar">
    <li role="none">
      <a href="/" aria-current="page" role="menuitem">ホーム</a>
    </li>
  </ul>
</nav>

<!-- 画像alt属性必須 -->
<img src="artwork.jpg" alt="夕焼けの美しい山の風景写真">
```

### ゲーミフィケーションUI実装
```typescript
// EXPバー実装（必須コンポーネント）
interface ExperienceBarProps {
  level: number;
  current: number;
  max: number;
  progress: number; // 0-1
  showDetails?: boolean;
}

// 使用例
<ExperienceBar 
  level={25} 
  current={12500} 
  max={15000} 
  progress={0.833} 
  showDetails={true} 
/>

// 表示形式: Lv.25 ■■■■■■■■□□ 12,500/15,000 EXP
```

### コンポーネント品質基準
```typescript
// TypeScript必須 - PropTypes禁止
interface ComponentProps {
  required: string;
  optional?: boolean;
  children?: ReactNode;
  className?: string; // Tailwind CSS用
  'aria-label'?: string; // アクセシビリティ用
}

// 関数コンポーネント推奨
const ComponentName: FC<ComponentProps> = ({
  required,
  optional = false,
  children,
  className = '',
  ...ariaProps
}) => {
  return (
    <div 
      className={`component-base ${className}`}
      {...ariaProps}
    >
      {children}
    </div>
  );
};
```

---

## �🧪 テスト・品質管理ルール

### テスト実行場所
```bash
# フロントエンド（frontend/フォルダで実行）

cd frontend/
npm run test
npm run test:e2e
npm run lint
npm run type-check

# バックエンド（backend/フォルダで実行）

cd backend/
python manage.py test
pytest
flake8 .
mypy .
```

### 品質ゲート
```yaml
必須チェック:
  - ビルドエラーなし
  - ユニットテスト通過率100%
  - ESLint/Flake8エラーなし
  - TypeScript/MyPyエラーなし
  - セキュリティスキャンクリア
```

---

## 📊 監視・ログルール

### ログレベル設定
```python
# backend/eldinia_nex/settings.py

LOGGING = {
    'version': 1,
    'handlers': {
        'console': {'level': 'DEBUG' if DEBUG else 'INFO'},
        'file': {'level': 'INFO', 'filename': 'logs/django.log'}
    }
}
```

### 監視項目
```yaml
パフォーマンス:
  - API応答時間: 500ms以下
  - ページロード時間: 2秒以下
  - エラー率: 1%以下

リソース:
  - CPU使用率: 80%以下
  - メモリ使用率: 90%以下
  - ディスク容量: 85%以下
```

---

## 🔒 セキュリティルール

### 環境変数管理（必須）
```bash
# .env（ローカル環境用）

SECRET_KEY=your-secret-key
DEBUG=True
DATABASE_TYPE=sqlite
ALLOWED_HOSTS=localhost,127.0.0.1

# .env.example（テンプレート）

SECRET_KEY=your-secret-key-here
DEBUG=True
DATABASE_TYPE=sqlite
ALLOWED_HOSTS=localhost,127.0.0.1
```

### 禁止事項
```yaml
絶対禁止:
  - パスワード・APIキーのハードコーディング
  - 機密情報のGitコミット
  - 本番環境でDEBUG=True
  - HTTP通信での機密データ送信
```

---

## 🚀 デプロイ・運用ルール

### デプロイ手順
```bash
# 1. プロダクション準備

cd backend/ && python manage.py collectstatic
cd ../frontend/ && npm run build

# 2. Docker本番ビルド

docker-compose -f docker-compose.yml build

# 3. 本番環境デプロイ

docker-compose -f docker-compose.yml up -d
```

### 運用監視
```yaml
必須監視:
  - アプリケーション稼働状況
  - データベース接続状況
  - API応答時間
  - エラー発生状況
  - セキュリティアラート
```

---

## 🔄 開発フロー

### 作業開始時（必須手順）
```bash
# 1. 最新コード同期

git checkout main && git pull origin main

# 2. 新ブランチ作成

git checkout -b feature/新機能名

# 3. 開発環境起動

cd backend/ && python manage.py runserver 0.0.0.0:8000 &
cd frontend/ && npm run dev &

# 4. 動作確認

curl http://localhost:8000/api/v1/health/
open http://localhost:3000
```

### 作業終了時（必須手順）
```bash
# 1. テスト実行

cd frontend/ && npm run test
cd ../backend/ && python manage.py test

# 2. コード品質チェック

cd frontend/ && npm run lint
cd ../backend/ && flake8 .

# 3. コミット・プッシュ

git add .
git commit -m "feat: 新機能の説明"
git push origin feature/新機能名

# 4. プルリクエスト作成
# GitHub/GitLabでプルリクエスト作成
```

---

## ⚠️ トラブルシューティング

### よくある問題と解決法

#### 1. Django起動エラー
```bash
# エラー: manage.py が見つからない
# 解決: 正しいディレクトリで実行

cd eldinia-nex/backend/  # ← 必須
python manage.py runserver
```

#### 2. Next.js起動エラー
```bash
# エラー: package.json が見つからない
# 解決: 正しいディレクトリで実行

cd eldinia-nex/frontend/  # ← 必須
npm run dev
```

#### 3. API接続エラー
```yaml
チェック項目:
  - Django: http://localhost:8000/api/v1/health/ にアクセス可能か
  - Next.js: CORS設定が正しく設定されているか
  - 両サーバーが同時に起動しているか
```

#### 4. PowerShell環境での問題
```powershell
# 作業ディレクトリ明示指定

Set-Location c:\eldinia-nex\backend
python manage.py runserver 0.0.0.0:8000 --noreload
```

---

## 📋 チェックリスト

### 日次開発チェックリスト

- [ ] 最新コードをpull済み
- [ ] backend/でDjango起動済み（ポート8000）
- [ ] frontend/でNext.js起動済み（ポート3000）
- [ ] API疎通確認済み（/api/v1/health/）
- [ ] ブラウザでフロントエンド確認済み
- [ ] 変更をコミット・プッシュ済み

### コードレビューチェックリスト

- [ ] 統合要件定義書に準拠
- [ ] コーディング規約遵守
- [ ] セキュリティチェック完了
- [ ] テストカバレッジ満足
- [ ] ドキュメント更新済み
- [ ] パフォーマンス影響なし

---

## 📚 関連ドキュメント

1. [統合要件定義書](./00_統合要件定義書.md) - プロジェクト全体仕様
2. [機能要件定義書](./01_機能要件定義書.md) - 機能詳細
3. [非機能要件定義書](./02_非機能要件定義書.md) - 性能・品質要件
4. [データベース設計書](./03_データベース設計書.md) - DB仕様
5. [API設計書](./04_API設計書.md) - API仕様
6. [UI/UX設計書](./05_UI_UX設計書.md) - デザイン仕様
7. [システム構成設計書](./06_システム構成設計書.md) - インフラ仕様

---

## 🔄 更新履歴

| バージョン | 日付 | 変更内容 | 担当者 |
|-----------|------|----------|--------|
| 1.0 | 2025-11-09 | 初版作成、基本ルール設定 | 開発チーム |

---

*この開発環境ルール設定書は、Eldonia-Nexプロジェクトの統合要件定義書に基づき、全開発者が遵守すべき標準ルールを定義します。規律ある開発プロセスにより、高品質なプラットフォームの構築を目指します。*

**重要**: このルールは全開発者必須遵守事項です。変更は開発チーム全体での合意が必要です。
