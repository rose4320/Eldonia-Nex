# Eldonia-Nex システム構成設計書

## 📋 ドキュメント情報

- **作成日**: 2025年11月3日

- **最終更新**: 2025年11月4日

- **バージョン**: 2.0 (段階的アーキテクチャ設計)

- **ステータス**: 設計完了・実装中

---

## 🎯 設計方針

### 段階的スケーリング戦略

Eldonia-Nexは **段階的アーキテクチャ移行** を採用し、サービス規模に応じて最適な構成に進化します。

```text
Phase 1 (現在)           Phase 2 (成長期)         Phase 3 (スケール期)
モノリシック            ハイブリッド           マイクロサービス
MVP・初期ユーザー      → 1万ユーザー以上      → 10万ユーザー以上
Next.js フルスタック     分離開始              完全分離

```text

---

## 1. Phase 1: モノリシックアーキテクチャ（現在）

### 1.1 現在の構成概要

**対象規模**: MVP ～ 10,000ユーザー

```mermaid
graph TB
    Client[ユーザー<br/>Web/Mobile]
    CDN[Vercel Edge Network<br/>CDN/SSR]

    subgraph "Next.js 15 アプリケーション"
        AppRouter[App Router]
        ServerComponents[Server Components<br/>SSR/ISR]
        ClientComponents[Client Components]
        APIRoutes[API Routes<br/>/app/api/*]
    end

    subgraph "データ層"
        PostgreSQL[(PostgreSQL 17+<br/>Django ORM)]
        Redis[(Redis 7.4+<br/>セッション/キャッシュ)]
        S3[Vercel Blob / CloudFlare R2<br/>ファイルストレージ]
    end

    subgraph "外部サービス"
        Stripe[Stripe<br/>決済]
        SendGrid[SendGrid<br/>メール]
        OAuth[OAuth Providers<br/>Google/GitHub/Twitter]
    end

    Client --> CDN
    CDN --> AppRouter
    AppRouter --> ServerComponents
    AppRouter --> ClientComponents
    AppRouter --> APIRoutes

    ServerComponents --> PostgreSQL
    ServerComponents --> Redis
    APIRoutes --> PostgreSQL
    APIRoutes --> Redis
    APIRoutes --> S3
    APIRoutes --> Stripe
    APIRoutes --> SendGrid
    APIRoutes --> OAuth

```text

### 1.2 技術スタック詳細

#### 1.2.1 フロントエンド（SSR優先）

```yaml
フレームワーク:
  - Next.js: 15.0+ (App Router必須)
  - React: 18.3+ (Server Components活用)
  - TypeScript: 5.7+ (strict mode)

スタイリング:
  - Tailwind CSS: 3.4+
  - Shadcn/UI: コンポーネントライブラリ
  - CSS Modules: スコープ化スタイル

状態管理:
  - React Context: グローバル状態
  - Zustand: クライアント状態（最小限）
  - Server State: Server Componentsで取得

SSR/SSG戦略:
  - トップページ: ISR (revalidate: 300秒)
  - 作品詳細: ISR (revalidate: 60秒)
  - ギャラリー: SSR (動的)
  - プロフィール: SSR (動的)
  - ダッシュボード: CSR (認証必須)

API通信:
  - Django REST APIとHTTP通信
  - Next.js Server Actionsでプロキシ
  - fetch() with revalidate対応

```text

#### 1.2.2 バックエンド（Django）

```yaml
フレームワーク:
  - Django: 5.1+ (LTS推奨)
  - Django REST Framework: 3.15+
  - Python: 3.12+

API設計:
  - RESTful API: Django REST Framework
  - ViewSets: モデルベースAPI
  - Serializers: バリデーション・シリアライズ
  - Routers: 自動URLルーティング

認証システム:
  - djangorestframework-simplejwt: JWT認証
  - django-allauth: OAuth 2.0 / SNS連携
  - django-cors-headers: CORS設定
  - django-otp: 2要素認証

ORM・データベース:
  - Django ORM: Pythonic クエリAPI
  - PostgreSQL 17+: メインDB
  - psycopg3: PostgreSQL ドライバ
  - django-redis: Redis統合

タスクキュー:
  - Celery: 非同期タスク処理
  - Redis: Celeryブローカー
  - Celery Beat: 定期実行タスク

キャッシュ戦略:
  - Redis: セッション・APIレスポンス
  - django.core.cache: Django標準キャッシュ
  - @cache_page デコレータ
  - select_related/prefetch_related: N+1対策

WebSocket・リアルタイム:
  - Django Channels: WebSocket対応
  - Redis: Channelレイヤーバックエンド
  - ASGI: 非同期サーバー対応

```text

#### 1.2.3 アーキテクチャ概要（Phase 1）

```mermaid
graph TB
    Client[ユーザー<br/>Web/Mobile]
    CDN[Vercel Edge Network<br/>CDN]

    subgraph "フロントエンド"
        NextJS[Next.js 15<br/>App Router + SSR]
        ServerComponents[Server Components]
        ClientComponents[Client Components]
    end

    subgraph "バックエンド（Django）"
        DjangoAPI[Django REST Framework<br/>API Server]
        DjangoAuth[認証・認可<br/>JWT + OAuth]
        DjangoORM[Django ORM]
        Celery[Celery<br/>非同期タスク]
        Channels[Django Channels<br/>WebSocket]
    end

    subgraph "データ層"
        PostgreSQL[(PostgreSQL 17+)]
        Redis[(Redis 7.4+<br/>Cache/Queue/Channels)]
        S3[CloudFlare R2<br/>ファイルストレージ]
    end

    Client --> CDN
    CDN --> NextJS
    NextJS --> ServerComponents
    NextJS --> ClientComponents

    NextJS -->|HTTP/REST| DjangoAPI
    ServerComponents -->|SSR Data Fetch| DjangoAPI
    ClientComponents -->|Client API Call| DjangoAPI

    DjangoAPI --> DjangoAuth
    DjangoAPI --> DjangoORM
    DjangoAPI --> Celery
    DjangoAPI --> Channels

    DjangoORM --> PostgreSQL
    DjangoAuth --> Redis
    Celery --> Redis
    Celery --> PostgreSQL
    Channels --> Redis
    DjangoAPI --> S3

```text

#### 1.2.3 データベース設計

```yaml
主要テーブル (Phase 1):
  ユーザー関連:
    - users: ユーザー基本情報
    - user_profiles: プロフィール詳細
    - user_exp_log: EXP履歴
    - user_badges: バッジ・実績
    - fans: ファン関係

  作品関連:
    - artworks: 作品情報
    - artwork_files: ファイルメタデータ
    - categories: カテゴリ
    - tags: タグ
    - artwork_tags: 作品-タグ関連
    - likes: いいね
    - comments: コメント

  チャレンジ関連（新規）:
    - challenges: お題情報
    - challenge_submissions: 投稿作品
    - challenge_participants: 参加者
    - challenge_rewards: 報酬履歴

  イベント関連:
    - events: イベント情報
    - event_tickets: チケット
    - event_orders: 注文履歴

  決済関連:
    - transactions: 取引履歴
    - orders: 注文
    - order_items: 注文明細
    - payouts: 出金履歴

  Live配信関連:
    - live_streams: 配信情報
    - stream_donations: 投げ銭
    - stream_viewers: 視聴者ログ

```text

### 1.3 Next.js 15 SSR 最適化戦略

#### 1.3.1 Server Components vs Client Components

```typescript
// ✅ Server Component（デフォルト）
// app/page.tsx - トップページ

async function HomePage() {
  // サーバーサイドでデータ取得
  const featuredArtworks = await prisma.artwork.findMany({
    where: { featured: true },
    take: 6,
    include: { creator: true }
  });

  const challenges = await prisma.challenge.findMany({
    where: { status: 'active' },
    orderBy: { created_at: 'desc' }
  });

  return (
    <Layout>
      <HeroSection />
      <ChallengesSlider challenges={challenges} /> {/* Client Component */}
      <FeaturedWorks artworks={featuredArtworks} />
    </Layout>
  );
}

// ✅ Client Component（最小限）
// app/components/ChallengesSlider.tsx
'use client';
import { useState } from 'react';

export function ChallengesSlider({ challenges }) {
  const [currentSlide, setCurrentSlide] = useState(0);
  // クライアントサイドのインタラクティブ機能のみ
  return (/* スライド UI */);
}

```text

#### 1.3.2 キャッシュ戦略詳細

```typescript
// Next.js 15 Cache API
// 静的データ（長期キャッシュ）
export async function getCategories() {
  const categories = await fetch('/api/categories', {
    next: { revalidate: 86400 } // 24時間
  });
  return categories.json();
}

// 動的データ（短期キャッシュ）
export async function getTrendingArtworks() {
  const artworks = await fetch('/api/artworks/trending', {
    next: { revalidate: 300 } // 5分
  });
  return artworks.json();
}

// リアルタイムデータ（キャッシュなし）
export async function getUserNotifications(userId: string) {
  const notifications = await fetch(`/api/users/${userId}/notifications`, {
    cache: 'no-store'
  });
  return notifications.json();
}

```text

#### 1.3.3 ISR（Incremental Static Regeneration）戦略

```typescript
// app/artworks/[id]/page.tsx
export const revalidate = 60; // 60秒ごとに再生成

export async function generateStaticParams() {
  // 人気作品のみ事前生成
  const artworks = await prisma.artwork.findMany({
    where: { views: { gte: 1000 } },
    select: { id: true },
    take: 100
  });

  return artworks.map((artwork) => ({
    id: artwork.id.toString()
  }));
}

async function ArtworkPage({ params }: { params: { id: string } }) {
  const artwork = await prisma.artwork.findUnique({
    where: { id: parseInt(params.id) },
    include: { creator: true, comments: true }
  });

  return <ArtworkDetail artwork={artwork} />;
}

```text

### 1.4 チャレンジ（お題）システム設計

#### 1.4.1 データベーススキーマ

```sql
-- お題マスター

CREATE TABLE challenges (
  id BIGSERIAL PRIMARY KEY,
  type VARCHAR(50) NOT NULL, -- 'monthly', 'weekly', 'daily', 'special'
  title VARCHAR(200) NOT NULL,
  description TEXT NOT NULL,
  icon VARCHAR(10), -- 絵文字
  color VARCHAR(50), -- Tailwind gradient class
  difficulty VARCHAR(20), -- 'beginner', 'intermediate', 'expert', 'all'
  reward_exp INTEGER NOT NULL DEFAULT 0,
  start_date TIMESTAMP NOT NULL,
  end_date TIMESTAMP,
  status VARCHAR(20) DEFAULT 'active', -- 'draft', 'active', 'closed'
  max_participants INTEGER,
  created_by BIGINT REFERENCES users(id),
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- 参加者

CREATE TABLE challenge_participants (
  id BIGSERIAL PRIMARY KEY,
  challenge_id BIGINT REFERENCES challenges(id) ON DELETE CASCADE,
  user_id BIGINT REFERENCES users(id) ON DELETE CASCADE,
  joined_at TIMESTAMP DEFAULT NOW(),
  status VARCHAR(20) DEFAULT 'participating', -- 'participating', 'completed', 'withdrawn'
  UNIQUE(challenge_id, user_id)
);

-- 投稿作品

CREATE TABLE challenge_submissions (
  id BIGSERIAL PRIMARY KEY,
  challenge_id BIGINT REFERENCES challenges(id) ON DELETE CASCADE,
  artwork_id BIGINT REFERENCES artworks(id) ON DELETE CASCADE,
  user_id BIGINT REFERENCES users(id) ON DELETE CASCADE,
  submitted_at TIMESTAMP DEFAULT NOW(),
  is_winner BOOLEAN DEFAULT FALSE,
  rank INTEGER,
  UNIQUE(challenge_id, artwork_id)
);

-- 報酬履歴

CREATE TABLE challenge_rewards (
  id BIGSERIAL PRIMARY KEY,
  challenge_id BIGINT REFERENCES challenges(id),
  user_id BIGINT REFERENCES users(id),
  exp_gained INTEGER NOT NULL,
  badge_id BIGINT REFERENCES user_badges(id),
  granted_at TIMESTAMP DEFAULT NOW()
);

-- インデックス

CREATE INDEX idx_challenges_type_status ON challenges(type, status);
CREATE INDEX idx_challenges_dates ON challenges(start_date, end_date);
CREATE INDEX idx_participants_user ON challenge_participants(user_id);
CREATE INDEX idx_submissions_challenge ON challenge_submissions(challenge_id);

```text

#### 1.4.2 API エンドポイント

```yaml
チャレンジAPI:
  GET /api/challenges:
    説明: お題一覧取得
    パラメータ:
      - status: active/closed
      - type: monthly/weekly/daily/special
      - limit: 件数
    レスポンス: お題配列 + 参加者数

  GET /api/challenges/{id}:
    説明: お題詳細取得
    レスポンス: お題情報 + 投稿作品一覧

  POST /api/challenges/{id}/join:
    説明: お題に参加
    認証: 必須
    処理: 参加登録 + EXP付与通知

  POST /api/challenges/{id}/submit:
    説明: 作品投稿
    認証: 必須
    パラメータ:
      - artwork_id: 作品ID
    処理: 投稿登録 + 報酬EXP付与

  GET /api/challenges/{id}/submissions:
    説明: 投稿作品一覧
    パラメータ:
      - sort: newest/popular/random

```text

### 1.5 ゲーミフィケーションシステム

#### 1.5.1 EXPシステム設計

```yaml
EXP獲得アクション:
  基本アクション:
    - ユーザー登録: 100 EXP
    - プロフィール完成: 50 EXP
    - メール認証: 30 EXP

  作品関連:
    - 作品投稿: 20 EXP
    - いいね獲得: 2 EXP (1件あたり)
    - コメント獲得: 5 EXP (1件あたり)
    - 作品販売: 売上の1% (最大1000 EXP)

  チャレンジ:
    - デイリー参加: 100 EXP
    - ウィークリー参加: 300 EXP
    - マンスリー参加: 500 EXP
    - スペシャル優勝: 2000 EXP

  ソーシャル:
    - ファン獲得: 10 EXP (10人ごと)
    - コメント投稿: 5 EXP
    - 作品シェア: 15 EXP

  イベント:
    - イベント参加: 100 EXP
    - イベント主催: 200 EXP
    - Live配信: 50 EXP/時間

レベル計算式:
  必要EXP = 1000 × (現在レベル ^ 1.5)

  例:
    Lv.1 → Lv.2: 1,000 EXP
    Lv.10 → Lv.11: 31,623 EXP
    Lv.25 → Lv.26: 125,000 EXP
    Lv.50 → Lv.51: 353,553 EXP

```text

#### 1.5.2 バッジ・実績システム

```sql
CREATE TABLE badge_definitions (
  id BIGSERIAL PRIMARY KEY,
  name VARCHAR(100) NOT NULL,
  description TEXT,
  icon VARCHAR(10), -- 絵文字
  category VARCHAR(50), -- 'beginner', 'artist', 'social', 'special'
  rarity VARCHAR(20), -- 'common', 'rare', 'epic', 'legendary'
  criteria JSONB NOT NULL, -- 獲得条件
  reward_exp INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE TABLE user_badges (
  id BIGSERIAL PRIMARY KEY,
  user_id BIGINT REFERENCES users(id) ON DELETE CASCADE,
  badge_id BIGINT REFERENCES badge_definitions(id),
  earned_at TIMESTAMP DEFAULT NOW(),
  progress INTEGER DEFAULT 100, -- 達成度（%）
  UNIQUE(user_id, badge_id)
);

-- バッジ例

INSERT INTO badge_definitions (name, description, icon, category, rarity, criteria, reward_exp) VALUES
('はじめの一歩', '最初の作品を投稿', '🎨', 'beginner', 'common', '{"artworks_count": 1}', 50),
('ベテランクリエイター', '100作品投稿達成', '👑', 'artist', 'epic', '{"artworks_count": 100}', 1000),
('人気者', 'ファン1000人達成', '⭐', 'social', 'rare', '{"fans_count": 1000}', 500),
('チャレンジマスター', 'お題10回参加', '🏆', 'special', 'epic', '{"challenges_completed": 10}', 800);

```text

### 1.6 多言語・多通貨対応

#### 1.6.1 i18n アーキテクチャ

```yaml
対応言語:
  - ja: 日本語（デフォルト）
  - en: English
  - ko: 한국어
  - zh: 中文（簡体字）
  - vi: Tiếng Việt

実装方法:
  - next-intl: Next.js 15 対応i18nライブラリ
  - /[locale]/* ルーティング
  - Server Components対応
  - 翻訳ファイル: JSON形式

ディレクトリ構造:
  /messages
    /ja.json - 日本語
    /en.json - 英語
    /ko.json - 韓国語
    /zh.json - 中国語
    /vi.json - ベトナム語

データベース多言語化:
  - artwork.title_translations: JSONB {"ja": "タイトル", "en": "Title"}
  - category.name_translations: JSONB
  - challenge.title_translations: JSONB

```text

#### 1.6.2 多通貨対応

```yaml
対応通貨:
  - JPY: 日本円（基準通貨）
  - USD: 米ドル
  - EUR: ユーロ
  - GBP: 英ポンド
  - KRW: 韓国ウォン
  - VND: ベトナムドン

為替レート管理:
  - API: Exchange Rates API / Open Exchange Rates
  - 更新頻度: 1日1回（深夜2:00）
  - キャッシュ: Redis (24時間)

価格計算:
  基準通貨: JPY
  変換式: 表示価格 = 基準価格 × 為替レート × (1 + 手数料)
  手数料: 2.5%

  例: ¥1,000の作品を米ドル表示
    為替レート: 1 USD = 150 JPY
    表示価格: $6.83 USD (1000 / 150 × 1.025)

データベース設計:
  CREATE TABLE exchange_rates (
    id BIGSERIAL PRIMARY KEY,
    base_currency VARCHAR(3) DEFAULT 'JPY',
    target_currency VARCHAR(3) NOT NULL,
    rate DECIMAL(10, 6) NOT NULL,
    updated_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(base_currency, target_currency)
  );

  CREATE TABLE user_currency_preferences (
    user_id BIGINT PRIMARY KEY REFERENCES users(id),
    preferred_currency VARCHAR(3) DEFAULT 'JPY',
    updated_at TIMESTAMP DEFAULT NOW()
  );

```text

### 1.7 デプロイメント（Phase 1）

```yaml
ホスティング:
  - Vercel: Next.js アプリケーション
  - Edge Functions: Vercel Edge Runtime
  - Serverless Functions: API Routes

データベース:
  - Vercel Postgres: PostgreSQL 17+
  - Upstash Redis: Redis 7.4+

ファイルストレージ:
  - Vercel Blob: 画像・動画
  - CloudFlare R2: 大容量ファイル

監視・ログ:
  - Vercel Analytics: パフォーマンス監視
  - Sentry: エラートラッキング
  - Vercel Logs: アプリケーションログ

CI/CD:
  - GitHub Actions: 自動テスト
  - Vercel: 自動デプロイ (Git push時)
  - Preview Deployments: PR毎のプレビュー環境

```text

---

## 2. Phase 2: ハイブリッドアーキテクチャ（成長期）

### 2.1 移行タイミング

**移行判断基準**:

- 月間アクティブユーザー: 10,000人以上

- 日次リクエスト: 100万件以上

- データベースサイズ: 100GB以上

- パフォーマンス問題: レスポンス3秒超過

### 2.2 分離優先順位

```text
優先度1: ファイル処理サービス分離
  - 理由: CPU/メモリ消費が大きい
  - 効果: メインアプリの負荷軽減

優先度2: Live配信サービス分離
  - 理由: リアルタイム処理・WebSocket必須
  - 効果: 独立スケーリング可能

優先度3: 決済サービス分離
  - 理由: セキュリティ要件・PCI DSS準拠
  - 効果: 監査・コンプライアンス対応

優先度4: 通知サービス分離
  - 理由: バックグラウンド処理
  - 効果: 非同期処理の最適化

```text

### 2.3 ハイブリッド構成図

```mermaid
graph TB
    Client[クライアント]
    CDN[CDN/Edge]

    subgraph "Next.js アプリケーション（メイン）"
        NextJS[Next.js 15<br/>UI/コア機能]
    end

    subgraph "分離サービス"
        FileService[ファイル処理<br/>Django/Celery]
        StreamService[Live配信<br/>Django Channels/WebSocket]
        PaymentService[決済<br/>Django/Stripe SDK]
        NotificationService[通知<br/>Django/Celery]
    end

    subgraph "データ層"
        PostgreSQL[(PostgreSQL<br/>Master)]
        PostgreSQLSlave[(PostgreSQL<br/>Slave)]
        Redis[(Redis Cluster)]
        S3[S3/R2]
        Queue[Message Queue<br/>Redis/BullMQ]
    end

    Client --> CDN
    CDN --> NextJS
    NextJS --> FileService
    NextJS --> StreamService
    NextJS --> PaymentService
    NextJS --> NotificationService

    NextJS --> PostgreSQL
    NextJS --> Redis
    FileService --> S3
    FileService --> PostgreSQL
    StreamService --> PostgreSQL
    StreamService --> Redis
    PaymentService --> PostgreSQL
    NotificationService --> Queue
    NotificationService --> PostgreSQLSlave

```text

---

## 3. Phase 3: マイクロサービスアーキテクチャ（スケール期）

### 3.1 完全マイクロサービス構成

**対象規模**: 100,000ユーザー以上

```mermaid
graph TB
    Client[クライアント]
    CDN[CDN]
    LB[ロードバランサー]

    subgraph "API Gateway"
        Gateway[Kong Gateway<br/>認証・ルーティング]
    end

    subgraph "マイクロサービス"
        UserService[ユーザー<br/>サービス]
        ArtworkService[作品<br/>サービス]
        ChallengeService[チャレンジ<br/>サービス]
        EventService[イベント<br/>サービス]
        StreamService[配信<br/>サービス]
        PaymentService[決済<br/>サービス]
        NotificationService[通知<br/>サービス]
        SearchService[検索<br/>サービス]
        AnalyticsService[分析<br/>サービス]
    end

    subgraph "データ層"
        PostgreSQLUser[(User DB)]
        PostgreSQLArtwork[(Artwork DB)]
        PostgreSQLChallenge[(Challenge DB)]
        PostgreSQLEvent[(Event DB)]
        RedisCluster[(Redis Cluster)]
        ElasticSearch[(ElasticSearch)]
        S3[S3]
        Kafka[Apache Kafka]
    end

    Client --> CDN
    CDN --> LB
    LB --> Gateway
    Gateway --> UserService
    Gateway --> ArtworkService
    Gateway --> ChallengeService
    Gateway --> EventService
    Gateway --> StreamService
    Gateway --> PaymentService

    UserService --> PostgreSQLUser
    ArtworkService --> PostgreSQLArtwork
    ChallengeService --> PostgreSQLChallenge
    EventService --> PostgreSQLEvent
    ArtworkService --> S3
    SearchService --> ElasticSearch

    NotificationService --> Kafka
    AnalyticsService --> Kafka

```text

### 3.2 サービス間通信

```yaml
同期通信（REST API）:
  - ユーザー情報取得
  - 作品メタデータ取得
  - 認証・認可確認

非同期通信（Message Queue）:
  - 作品投稿通知
  - EXP付与イベント
  - メール送信
  - 統計データ更新

Event Sourcing:
  - ユーザーアクションログ
  - 決済トランザクション
  - 監査ログ

```text

---

## 4. セキュリティ設計

### 4.1 認証・認可

```yaml
認証方式:
  - Next-Auth v5: セッション管理
  - JWT: Access Token (1時間)
  - Refresh Token: HttpOnly Cookie (30日)
  - OAuth 2.0: Google/GitHub/Twitter

多要素認証:
  - TOTP: Google Authenticator対応
  - SMS: Twilio統合
  - Email: 確認コード送信

ロール・権限:
  roles:
    - guest: 閲覧のみ
    - user: 基本機能
    - creator: 作品投稿
    - premium: 有料機能
    - moderator: コンテンツ管理
    - admin: システム管理

  permissions:
    - read:artworks
    - write:artworks
    - delete:artworks
    - manage:users
    - manage:challenges
    - manage:events

```text

### 4.2 データ保護

```yaml
暗号化:
  - TLS 1.3: すべての通信
  - AES-256: データベース
  - bcrypt: パスワード (cost=12)

個人情報保護:
  - GDPR準拠: EU圏ユーザー
  - 個人情報保護法: 日本ユーザー
  - データ削除権: アカウント削除時
  - データポータビリティ: エクスポート機能

セキュリティヘッダー:
  Strict-Transport-Security: "max-age=31536000; includeSubDomains"
  Content-Security-Policy: "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'"
  X-Frame-Options: "DENY"
  X-Content-Type-Options: "nosniff"
  Referrer-Policy: "strict-origin-when-cross-origin"

```text

### 4.3 Rate Limiting

```yaml
API制限:
  未認証:
    - 100 req/hour/IP (全体)
    - 10 req/minute/IP (認証エンドポイント)

  認証済み:
    - 1000 req/hour/user (読取API)
    - 100 req/hour/user (書込API)
    - 10 req/hour/user (ファイルアップロード)

  Premium:
    - 5000 req/hour/user
    - 500 req/hour/user (書込)
    - 50 req/hour/user (アップロード)

実装:
  - Upstash Rate Limit: Edge対応
  - Redis: カウンター管理
  - レスポンスヘッダー: X-RateLimit-*

```text

---

## 5. パフォーマンス最適化

### 5.1 キャッシュ戦略

```yaml
多層キャッシュ:
  Layer 1 - ブラウザキャッシュ:
    - 静的アセット: 1年
    - 画像: 30日
    - API: 5分

  Layer 2 - CDN キャッシュ（Edge）:
    - 静的ページ: 1時間
    - 画像: 7日
    - API (GET): 5分

  Layer 3 - Redis キャッシュ:
    - ユーザープロフィール: 1時間
    - 作品メタデータ: 30分
    - カテゴリ一覧: 24時間
    - ランキング: 15分

  Layer 4 - Next.js Data Cache:
    - fetch() with revalidate
    - generateStaticParams()
    - ISR

キャッシュ無効化:
  - 作品更新 → 作品キャッシュ削除
  - プロフィール更新 → ユーザーキャッシュ削除
  - お題追加 → お題一覧キャッシュ削除
  - Revalidate On-Demand: Next.js revalidatePath()

```text

### 5.2 データベース最適化

```sql
-- インデックス戦略

CREATE INDEX CONCURRENTLY idx_artworks_featured
ON artworks(created_at DESC)
WHERE featured = TRUE AND status = 'published';

CREATE INDEX CONCURRENTLY idx_challenges_active
ON challenges(start_date, end_date)
WHERE status = 'active';

CREATE INDEX CONCURRENTLY idx_user_exp_log_monthly
ON user_exp_log(user_id, created_at DESC)
WHERE created_at >= CURRENT_DATE - INTERVAL '30 days';

-- パーティショニング（時系列データ）

CREATE TABLE user_exp_log (
  id BIGSERIAL,
  user_id BIGINT NOT NULL,
  exp_gained INTEGER NOT NULL,
  source VARCHAR(50),
  created_at TIMESTAMP NOT NULL DEFAULT NOW()
) PARTITION BY RANGE (created_at);

CREATE TABLE user_exp_log_2025_11 PARTITION OF user_exp_log
FOR VALUES FROM ('2025-11-01') TO ('2025-12-01');

CREATE TABLE user_exp_log_2025_12 PARTITION OF user_exp_log
FOR VALUES FROM ('2025-12-01') TO ('2026-01-01');

-- クエリ最適化
-- ❌ 遅い: N+1クエリ

SELECT * FROM artworks;
-- 各artwork毎にcreator取得

-- ✅ 速い: JOIN

SELECT a.*, u.display_name, u.avatar_url
FROM artworks a
LEFT JOIN users u ON a.creator_id = u.id
WHERE a.status = 'published'
ORDER BY a.created_at DESC
LIMIT 20;

-- ✅ さらに速い: マテリアライズドビュー

CREATE MATERIALIZED VIEW trending_artworks AS
SELECT
  a.*,
  u.display_name,
  u.avatar_url,
  COUNT(DISTINCT l.id) as likes_count,
  COUNT(DISTINCT c.id) as comments_count
FROM artworks a
LEFT JOIN users u ON a.creator_id = u.id
LEFT JOIN likes l ON a.id = l.artwork_id AND l.created_at >= NOW() - INTERVAL '7 days'

LEFT JOIN comments c ON a.id = c.artwork_id
WHERE a.status = 'published'
GROUP BY a.id, u.display_name, u.avatar_url
ORDER BY likes_count DESC, comments_count DESC;

-- 毎時更新

REFRESH MATERIALIZED VIEW CONCURRENTLY trending_artworks;

```text

### 5.3 画像最適化

```yaml
アップロード時処理:
  1. ウイルススキャン: ClamAV
  2. フォーマット検証: MIME Type確認
  3. サイズ制限: 最大100MB
  4. メタデータ抽出: Exif情報
  5. 自動リサイズ: 複数サイズ生成

生成サイズ:
  - thumbnail: 200x200px (WebP + JPEG)
  - small: 400x400px
  - medium: 800x600px
  - large: 1200x900px
  - original: そのまま保存

WebP変換:
  - 品質: 85%
  - プログレッシブ: 有効
  - フォールバック: JPEG

CDN配信:
  - CloudFlare Images: 自動最適化
  - Lazy Loading: loading="lazy"
  - Responsive Images: srcset対応

```text

---

## 6. 監視・ログ

### 6.1 監視ツール

```yaml
アプリケーション監視:
  - Vercel Analytics: Core Web Vitals
  - Sentry: エラートラッキング
  - LogRocket: ユーザーセッションリプレイ

インフラ監視:
  - Vercel Dashboard: サーバーレス関数
  - Upstash Console: Redis統計
  - Vercel Postgres Insights: DB パフォーマンス

ビジネスメトリクス:
  - Mixpanel: ユーザー行動分析
  - Google Analytics 4: トラフィック分析
  - Stripe Dashboard: 決済・売上

```text

### 6.2 アラート設定

```yaml
Critical（即座対応）:
  - エラー率 > 5%: Slack通知
  - レスポンス時間 > 3秒: Slack通知
  - データベースダウン: 電話 + Slack
  - 決済処理失敗率 > 1%: 電話 + Slack

Warning（1時間以内）:
  - エラー率 > 2%: Email
  - レスポンス時間 > 2秒: Slack
  - Redis メモリ使用率 > 80%: Slack
  - ストレージ使用率 > 85%: Email

Info（参考情報）:
  - デプロイ完了: Slack
  - 日次バックアップ完了: Email
  - 週次レポート: Email

```text

### 6.3 ログ設計

```json
{
  "timestamp": "2025-11-04T12:00:00.000Z",
  "level": "info",
  "service": "next-app",
  "trace_id": "abc123def456",
  "user_id": 12345,
  "request_id": "req_xyz789",
  "method": "POST",
  "path": "/api/artworks",
  "status": 201,
  "duration_ms": 245,
  "user_agent": "Mozilla/5.0...",
  "ip": "192.168.1.1",
  "action": "artwork_created",
  "metadata": {
    "artwork_id": 67890,
    "file_size": 8500000,
    "category": "illustration"
  },
  "message": "Artwork created successfully"
}

```text

---

## 7. バックアップ・災害対策

### 7.1 バックアップ戦略

```yaml
データベース:
  - 自動バックアップ: 日次（深夜2:00）
  - 保持期間: 35日間
  - Point-in-Time Recovery: 有効
  - クロスリージョン: Singapore（DR用）

ファイルストレージ:
  - バージョニング: 有効（90日間）
  - クロスリージョンレプリケーション: 有効
  - Lifecycle Policy:
      30日後 → Infrequent Access
      1年後 → Glacier

コード:
  - Git: GitHub（プライベートリポジトリ）
  - バックアップ: GitLab（ミラーリング）
  - Release Tag: 本番デプロイ毎

```text

### 7.2 災害復旧計画

```yaml
RTO（目標復旧時間）: 4時間
RPO（目標復旧時点）: 1時間

災害シナリオ:
  1. データベース障害:
     - Slave DBへフェイルオーバー（自動）
     - 復旧時間: 5分以内

  2. リージョン障害:
     - DRリージョンへ切替（手動）
     - 復旧時間: 2時間

  3. 完全障害:
     - バックアップから復元
     - 復旧時間: 4時間

復旧手順:
  1. インシデント検知
  2. 影響範囲確認
  3. バックアップ選択
  4. DR環境起動
  5. DNS切替
  6. 動作確認
  7. ユーザー通知

```text

---

## 8. 技術スタック一覧

### 8.1 Phase 1（現在）

| カテゴリ | 技術 | バージョン | 用途 |
|---------|------|----------|------|
| **フロントエンド** | Next.js | 15.0+ | フルスタックフレームワーク |
| | React | 18.3+ | UIライブラリ |
| | TypeScript | 5.7+ | 型安全性 |
| | Tailwind CSS | 3.4+ | スタイリング |
| | Shadcn/UI | Latest | コンポーネント |
| **バックエンド** | Django | 5.1+ | メインフレームワーク |
| | Django REST Framework | 3.15+ | RESTful API |
| | Django ORM | - | データベースORM |

| | djangorestframework-simplejwt | 5.3+ | JWT認証 |
| | django-allauth | 0.63+ | OAuth 2.0 |
| **データベース** | PostgreSQL | 17+ | メインDB |
| | Redis | 7.4+ | キャッシュ・セッション |
| **ストレージ** | Vercel Blob | - | ファイルストレージ |

| | CloudFlare R2 | - | 大容量ストレージ |

| **ホスティング** | Vercel | - | Next.jsホスティング |

| **監視** | Vercel Analytics | - | パフォーマンス監視 |

| | Sentry | Latest | エラートラッキング |
| **決済** | Stripe | Latest | 決済処理 |
| **メール** | SendGrid | Latest | メール配信 |

### 8.2 Phase 2（成長期）

| カテゴリ | 技術 | バージョン | 用途 |
|---------|------|----------|------|
| **追加サービス** | FastAPI | 0.115+ | ファイル処理 |
| | Socket.io | 4.8+ | Live配信 |
| | BullMQ | 5.0+ | ジョブキュー |
| **インフラ** | Docker | 27.3+ | コンテナ化 |
| | AWS ECS | - | コンテナオーケストレーション |

| **データベース** | PostgreSQL（Read Replica） | 17+ | 読取専用DB |

### 8.3 Phase 3（スケール期）

| カテゴリ | 技術 | バージョン | 用途 |
|---------|------|----------|------|
| **API Gateway** | Kong | 3.8+ | APIゲートウェイ |
| **オーケストレーション** | Kubernetes | 1.31+ | コンテナ管理 |
| **メッセージング** | Apache Kafka | 3.8+ | イベントストリーミング |
| **検索** | ElasticSearch | 8.15+ | 全文検索 |
| **監視** | Datadog | - | 統合監視 |

| | Prometheus | Latest | メトリクス収集 |
| | Grafana | Latest | ダッシュボード |

---

## 9. 開発・運用環境

### 9.1 環境構成

```yaml
Development（開発環境）:
  - ローカル開発: Docker Compose
  - データベース: PostgreSQL（ローカル）
  - Redis: Redis（ローカル）
  - テストデータ: Django Fixtures / Management Commands
  - ホットリロード: Django Dev Server / Next.js Dev Server

Staging（ステージング環境）:
  - Vercel Preview Deployment
  - PostgreSQL: Vercel Postgres（Dev）
  - Redis: Upstash Redis（Dev）
  - 自動デプロイ: PRマージ時
  - E2Eテスト: Playwright

Production（本番環境）:
  - Vercel Production
  - PostgreSQL: Vercel Postgres（Pro）
  - Redis: Upstash Redis（Pro）
  - 自動デプロイ: mainブランチpush時
  - ブルーグリーンデプロイ: Vercel標準

```text

### 9.2 Docker構成

#### 9.2.1 Django Dockerfile

```dockerfile

## backend/Dockerfile

FROM python:3.12-slim

## 環境変数設定

ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1 \
    PIP_DISABLE_PIP_VERSION_CHECK=1

## 作業ディレクトリ設定

WORKDIR /app

## システムパッケージインストール

RUN apt-get update && apt-get install -y \
    postgresql-client \
    gcc \
    python3-dev \
    musl-dev \
    libpq-dev \
    && rm -rf /var/lib/apt/lists/*

## Python依存関係インストール

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

## アプリケーションコードコピー

COPY . .

## 静的ファイル収集

RUN python manage.py collectstatic --noinput

## 非rootユーザー作成

RUN useradd -m -u 1000 django && chown -R django:django /app
USER django

## ヘルスチェック

HEALTHCHECK --interval=30s --timeout=10s --start-period=40s --retries=3 \
    CMD python -c "import requests; requests.get('<http://localhost:8000/health/>')"

## ポート公開

EXPOSE 8000

## 起動コマンド

CMD ["gunicorn", "--bind", "0.0.0.0:8000", "--workers", "4", "--threads", "2", "config.wsgi:application"]

```text

#### 9.2.2 Next.js Dockerfile

```dockerfile

## frontend/Dockerfile

FROM node:22-alpine AS base

## 依存関係インストール

FROM base AS deps
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci

## ビルド

FROM base AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .
ENV NEXT_TELEMETRY_DISABLED 1
RUN npm run build

## 本番環境

FROM base AS runner
WORKDIR /app

ENV NODE_ENV production
ENV NEXT_TELEMETRY_DISABLED 1

RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

USER nextjs

EXPOSE 3000

ENV PORT 3000

CMD ["node", "server.js"]

```text

#### 9.2.3 docker-compose.yml（開発環境）

```yaml
version: '3.9'

services:
  # PostgreSQL
  postgres:
    image: postgres:17-alpine
    container_name: eldonia-postgres
    environment:
      POSTGRES_DB: eldonia_nex
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --locale=ja_JP.UTF-8"
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - eldonia-network

  # Redis
  redis:
    image: redis:7.4-alpine
    container_name: eldonia-redis
    command: redis-server --appendonly yes --requirepass redis_password
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - eldonia-network

  # Django Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: eldonia-backend
    command: >
      sh -c "python manage.py migrate &&
             python manage.py runserver 0.0.0.0:8000"
    environment:
      - DEBUG=True
      - SECRET_KEY=dev-secret-key-change-in-production
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/eldonia_nex
      - REDIS_URL=redis://:redis_password@redis:6379/0
      - CELERY_BROKER_URL=redis://:redis_password@redis:6379/0
      - ALLOWED_HOSTS=localhost,127.0.0.1,backend
      - CORS_ALLOWED_ORIGINS=<http://localhost:3000>
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - backend_static:/app/staticfiles
      - backend_media:/app/media
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - eldonia-network

  # Celery Worker
  celery:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: eldonia-celery
    command: celery -A config worker -l info --concurrency=4
    environment:
      - DEBUG=True
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/eldonia_nex
      - REDIS_URL=redis://:redis_password@redis:6379/0
      - CELERY_BROKER_URL=redis://:redis_password@redis:6379/0
    volumes:
      - ./backend:/app
    depends_on:
      - postgres
      - redis
      - backend
    networks:
      - eldonia-network

  # Celery Beat
  celery-beat:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: eldonia-celery-beat
    command: celery -A config beat -l info --scheduler django_celery_beat.schedulers:DatabaseScheduler
    environment:
      - DEBUG=True
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/eldonia_nex
      - REDIS_URL=redis://:redis_password@redis:6379/0
      - CELERY_BROKER_URL=redis://:redis_password@redis:6379/0
    volumes:
      - ./backend:/app
    depends_on:
      - postgres
      - redis
      - backend
    networks:
      - eldonia-network

  # Django Channels (WebSocket)
  channels:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: eldonia-channels
    command: daphne -b 0.0.0.0 -p 8001 config.asgi:application
    environment:
      - DEBUG=True
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/eldonia_nex
      - REDIS_URL=redis://:redis_password@redis:6379/1
    ports:
      - "8001:8001"
    volumes:
      - ./backend:/app
    depends_on:
      - postgres
      - redis
    networks:
      - eldonia-network

  # Next.js Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: eldonia-frontend
    command: npm run dev
    environment:
      - NODE_ENV=development
      - NEXT_PUBLIC_API_URL=<http://localhost:8000/api>
      - NEXT_PUBLIC_WS_URL=ws://localhost:8001/ws
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - backend
    networks:
      - eldonia-network

  # Nginx (リバースプロキシ)
  nginx:
    image: nginx:alpine
    container_name: eldonia-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - backend_static:/var/www/static:ro
      - backend_media:/var/www/media:ro
    depends_on:
      - backend
      - frontend
    networks:
      - eldonia-network

volumes:
  postgres_data:
  redis_data:
  backend_static:
  backend_media:

networks:
  eldonia-network:
    driver: bridge

```text

#### 9.2.4 docker-compose.prod.yml（本番環境）

```yaml
version: '3.9'

services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always
    networks:
      - eldonia-network

  redis:
    image: redis:7.4-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD} --maxmemory 2gb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    restart: always
    networks:
      - eldonia-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 4 --threads 2 --timeout 60
    environment:
      - DEBUG=False
      - SECRET_KEY=${SECRET_KEY}
      - DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
      - ALLOWED_HOSTS=${ALLOWED_HOSTS}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS}
    volumes:
      - backend_static:/app/staticfiles
      - backend_media:/app/media
    depends_on:
      - postgres
      - redis
    restart: always
    networks:
      - eldonia-network

  celery:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: celery -A config worker -l info --concurrency=8
    environment:
      - DEBUG=False
      - DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}
      - CELERY_BROKER_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
    depends_on:
      - postgres
      - redis
    restart: always
    networks:
      - eldonia-network

  channels:
    build:
      context: ./backend
      dockerfile: Dockerfile
    command: daphne -b 0.0.0.0 -p 8001 config.asgi:application
    environment:
      - DEBUG=False
      - DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@postgres:5432/${DB_NAME}
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/1
    depends_on:
      - postgres
      - redis
    restart: always
    networks:
      - eldonia-network

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.prod.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - backend_static:/var/www/static:ro
      - backend_media:/var/www/media:ro
    depends_on:
      - backend
    restart: always
    networks:
      - eldonia-network

volumes:
  postgres_data:
  redis_data:
  backend_static:
  backend_media:

networks:
  eldonia-network:
    driver: bridge

```text

#### 9.2.5 環境変数設定（.env.example）

```bash

## Django設定

DEBUG=True
SECRET_KEY=your-secret-key-here
ALLOWED_HOSTS=localhost,127.0.0.1

## データベース

DB_NAME=eldonia_nex
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=postgres
DB_PORT=5432

## Redis

REDIS_PASSWORD=redis_password
REDIS_HOST=redis
REDIS_PORT=6379

## Celery

CELERY_BROKER_URL=redis://:redis_password@redis:6379/0
CELERY_RESULT_BACKEND=redis://:redis_password@redis:6379/0

## CORS設定

CORS_ALLOWED_ORIGINS=<http://localhost:3000,http://localhost:80>

## AWS S3（本番環境）

AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_STORAGE_BUCKET_NAME=eldonia-media
AWS_S3_REGION_NAME=ap-northeast-1

## Stripe決済

STRIPE_PUBLIC_KEY=pk_test_xxx
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx

## SendGrid

SENDGRID_API_KEY=SG.xxx

## Sentry

SENTRY_DSN=<https://xxx@sentry.io/xxx>

```text

#### 9.2.6 Docker コマンド一覧

```bash

## 開発環境起動

docker-compose up -d

## ログ確認

docker-compose logs -f backend
docker-compose logs -f celery

## コンテナ一覧

docker-compose ps

## マイグレーション実行

docker-compose exec backend python manage.py migrate

## スーパーユーザー作成

docker-compose exec backend python manage.py createsuperuser

## 静的ファイル収集

docker-compose exec backend python manage.py collectstatic --noinput

## テスト実行

docker-compose exec backend python manage.py test

## シェル起動

docker-compose exec backend python manage.py shell

## データベースシェル

docker-compose exec backend python manage.py dbshell

## コンテナ停止・削除

docker-compose down

## ボリューム含めて完全削除

docker-compose down -v

## 本番環境デプロイ

docker-compose -f docker-compose.prod.yml up -d

```text

### 9.3 CI/CD パイプライン（Django + Next.js）

```yaml
name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  # Djangoバックエンドテスト
  backend-test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_DB: test_db
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

      redis:
        image: redis:7.4-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Install dependencies
        run: |
          cd backend
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run linting
        run: |
          cd backend
          flake8 .
          black --check .
          isort --check-only .

      - name: Run migrations
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
          REDIS_URL: redis://localhost:6379/0
        run: |
          cd backend
          python manage.py migrate

      - name: Run tests
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/test_db
          REDIS_URL: redis://localhost:6379/0
        run: |
          cd backend
          python manage.py test
          coverage run --source='.' manage.py test
          coverage report
          coverage xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./backend/coverage.xml
          flags: backend

  # Next.jsフロントエンドテスト
  frontend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: |
          cd frontend
          npm ci

      - name: Run linting
        run: |
          cd frontend
          npm run lint
          npm run type-check

      - name: Run tests
        run: |
          cd frontend
          npm test
          npm run test:coverage

  # E2Eテスト
  e2e-test:
    needs: [backend-test, frontend-test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Start services
        run: |
          docker-compose up -d
          sleep 30  # サービス起動待機

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Playwright
        run: |
          cd frontend
          npm ci
          npx playwright install --with-deps

      - name: Run E2E tests
        run: |
          cd frontend
          npm run test:e2e

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: frontend/playwright-report/

      - name: Cleanup
        if: always()
        run: docker-compose down -v

  # Dockerイメージビルド
  build-docker:
    needs: [backend-test, frontend-test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/eldonia-backend:latest
            ${{ secrets.DOCKER_USERNAME }}/eldonia-backend:${{ github.sha }}
          cache-from: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/eldonia-backend:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKER_USERNAME }}/eldonia-backend:buildcache,mode=max

      - name: Build and push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/eldonia-frontend:latest
            ${{ secrets.DOCKER_USERNAME }}/eldonia-frontend:${{ github.sha }}

  # ステージング環境デプロイ
  deploy-staging:
    needs: [e2e-test, build-docker]
    if: github.ref == 'refs/heads/develop'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to AWS ECS (Staging)
        run: |
          aws ecs update-service \
            --cluster eldonia-staging \
            --service backend \
            --force-new-deployment
          aws ecs update-service \
            --cluster eldonia-staging \
            --service frontend \
            --force-new-deployment
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ap-northeast-1

  # 本番環境デプロイ
  deploy-production:
    needs: [e2e-test, build-docker]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: production
      url: <https://eldonia-nex.com>
    steps:
      - uses: actions/checkout@v4

      - name: Run database migrations
        run: |
          aws ecs run-task \
            --cluster eldonia-production \
            --task-definition migrate-task \
            --launch-type FARGATE \
            --network-configuration "awsvpcConfiguration={subnets=[subnet-xxx],securityGroups=[sg-xxx],assignPublicIp=ENABLED}"
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ap-northeast-1

      - name: Deploy to AWS ECS (Production)
        run: |
          aws ecs update-service \
            --cluster eldonia-production \
            --service backend \
            --force-new-deployment
          aws ecs update-service \
            --cluster eldonia-production \
            --service frontend \
            --force-new-deployment
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_DEFAULT_REGION: ap-northeast-1

      - name: Notify Slack
        uses: 8398a7/action-slack@v3
        with:
          status: ${{ job.status }}
          text: 'Production deployment completed!'
          webhook_url: ${{ secrets.SLACK_WEBHOOK }}
        if: always()

```text

---

## 10. 今後の拡張計画

### 10.1 短期（3-6ヶ月）

```yaml
機能追加:
  - ✅ お題（チャレンジ）システム（実装済み）
  - ⏳ Live配信機能（WebRTC）
  - ⏳ NFT連携（Ethereum/Polygon）
  - ⏳ AI画像生成統合（Stable Diffusion）

最適化:
  - ⏳ ISR範囲拡大
  - ⏳ Edge Functions活用
  - ⏳ 画像最適化（WebP/AVIF）
  - ⏳ データベースインデックス最適化

```text

### 10.2 中期（6-12ヶ月）

```yaml
スケーリング:
  - Phase 2移行準備
  - ファイル処理サービス分離
  - Redis Cluster導入
  - Read Replica追加

新機能:
  - モバイルアプリ（React Native）
  - サブスクリプション（月額会員）
  - クリエイターグッズ販売
  - AR/VRギャラリー

```text

### 10.3 長期（12ヶ月以上）

```yaml
完全マイクロサービス化:
  - Phase 3移行
  - Kubernetes導入
  - グローバル展開（多リージョン）
  - AI推薦システム

エコシステム拡大:
  - Partner API公開
  - ホワイトラベル提供
  - B2B SaaS展開

```text

---

## 📚 関連ドキュメント

- [01_機能要件定義書.md](./01_機能要件定義書.md) - 機能詳細仕様

- [02_非機能要件定義書.md](./02_非機能要件定義書.md) - パフォーマンス・セキュリティ要件

- [03_データベース設計書.md](./03_データベース設計書.md) - DB設計・ER図

- [04_API設計書.md](./04_API設計書.md) - API仕様

- [05_UI_UX設計書.md](./05_UI_UX設計書.md) - UI/UXデザイン

---

## 🔄 更新履歴

- 2025-11-04: v2.0 - 段階的アーキテクチャ設計追加、チャレンジシステム追加、SSR最適化戦略詳細化

- 2025-11-03: v1.1 - 技術スタック更新

- 2025-11-03: v1.0 - 初版作成

---

*このドキュメントは開発進捗に応じて随時更新されます。*
