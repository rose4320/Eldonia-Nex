# 多言語・多通貨対応 更新サマリー

> **更新日: 2025年11月3日**

## 📋 更新対象ファイル

### ✅ 更新完了

- `docs/02_非機能要件定義書.md` (v1.1 → v1.2)
- `docs/01_機能要件定義書.md` (v1.1 → v1.2)
- `docs/04_API設計書.md` (v1.1 → v1.2)

## 🌍 多言語対応更新

### 対応言語拡張

| 段階                | 言語             | 言語コード | 備考         |
| ------------------- | ---------------- | ---------- | ------------ |
| **フェーズ1** | 日本語           | `ja`     | メイン言語   |
|                     | 英語             | `en`     | 国際展開     |
| **フェーズ2** | 韓国語           | `ko`     | 追加対応     |
|                     | 中国語（簡体字） | `zh-CN`  | 追加対応     |
|                     | ベトナム語       | `vi`     | 追加対応     |
| **将来**      | アラビア語       | `ar`     | 右から左言語 |

### 実装内容

- **言語設定API**: 対応言語取得・ユーザー言語設定
- **コンテンツ多言語化**: 作品タイトル・説明の多言語表示
- **UI多言語化**: インターフェース要素の翻訳対応

## 💰 多通貨対応更新

### 対応通貨拡張

| 通貨         | 通貨コード | シンボル | 小数点以下 | 地域           |
| ------------ | ---------- | -------- | ---------- | -------------- |
| 日本円       | `JPY`    | ¥       | 0桁        | 日本           |
| 米ドル       | `USD`    | $        | 2桁        | アメリカ・国際 |
| ユーロ       | `EUR`    | €       | 2桁        | ヨーロッパ     |
| 韓国ウォン   | `KRW`    | ₩       | 0桁        | 韓国           |
| 中国元       | `CNY`    | ¥       | 2桁        | 中国           |
| ベトナムドン | `VND`    | ₫       | 0桁        | ベトナム       |

### 実装機能

- **通貨設定API**: 対応通貨取得・ユーザー通貨設定
- **為替レートAPI**: リアルタイム為替レート取得
- **価格変換API**: 通貨間の自動価格変換
- **決済多通貨対応**: 各通貨での決済処理

## 🔧 技術実装詳細

### API エンドポイント追加

#### 多言語関連

```http
GET /api/localization/languages     # 対応言語一覧
PUT /api/users/me/language          # ユーザー言語設定
GET /api/artworks/{id}?lang=ko      # 多言語コンテンツ取得
```

#### 多通貨関連

```http
GET /api/currencies                 # 対応通貨一覧
GET /api/currencies/rates           # 為替レート取得
GET /api/currencies/convert         # 価格変換
GET /api/users/me/locale            # 地域設定取得
```

### データベース設計影響

#### 新規テーブル追加

- `languages`: 対応言語マスタ
- `currencies`: 対応通貨マスタ
- `exchange_rates`: 為替レート履歴
- `content_translations`: コンテンツ翻訳データ

#### 既存テーブル拡張

```sql
-- ユーザーテーブルに地域設定追加
ALTER TABLE users ADD COLUMN preferred_language VARCHAR(5) DEFAULT 'ja';
ALTER TABLE users ADD COLUMN preferred_currency VARCHAR(3) DEFAULT 'JPY';
ALTER TABLE users ADD COLUMN timezone VARCHAR(50) DEFAULT 'Asia/Tokyo';

-- 作品テーブルに多言語対応
ALTER TABLE artworks ADD COLUMN original_language VARCHAR(5) DEFAULT 'ja';

-- 価格テーブルに通貨情報追加
ALTER TABLE prices ADD COLUMN currency VARCHAR(3) DEFAULT 'JPY';
```

## 🎯 ビジネス効果

### グローバル展開準備

- **市場拡大**: アジア圏5カ国への展開基盤
- **ユーザービリティ向上**: 母国語・現地通貨でのサービス利用
- **売上機会増加**: 多通貨決済による購入障壁削減

### 運用メリット

- **コンテンツ管理**: 言語別コンテンツの効率的管理
- **分析改善**: 地域別・言語別の利用統計取得
- **マーケティング**: 地域特性に応じた施策展開

## ⚠️ 注意事項・今後の課題

### 実装時の注意点

1. **文字数制限**: 翻訳後の文字数変化への対応
2. **文化的配慮**: 地域別の文化・慣習への配慮
3. **法的対応**: 各国の法規制・税制への対応
4. **品質管理**: 翻訳品質の維持・チェック体制 — [`docs/translation-architecture.md`](./translation-architecture.md) § Quality Assurance（Translation Quality Agent = 校正チェック係。UGC 全件人力校正は MVP 対象外）

### 今後の拡張予定

- **機械翻訳**: AI翻訳機能の統合
- **地域限定機能**: 地域別特別機能の提供
- **現地化支援**: 各地域のパートナー連携
- **多通貨決済**: 仮想通貨対応の検討

---

## 2026-07-13 更新 — 翻訳 Nexus 本番 + 戦略

**詳細正本**: `docs/translation-architecture.md` v1.1

### 実装済み

| 項目 | 内容 |
|------|------|
| Google Cloud Translation API | 本番（Vercel `GOOGLE_TRANSLATE_API_KEY`） |
| API | `POST /api/nexus/translate` |
| UI | Community スレッド・返信（翻訳 Nexus パネル） |
| 対応 UI 言語 | `ja`, `en`, `ko`, `zh-CN` |

### 採用方針（未実装含む）

1. **翻訳ベース表示** — 海外ユーザーは訳文を主、原文は薄く折りたたみ
2. **投稿時翻訳 + DB キャッシュ** — 閲覧のたび API を呼ばない
3. **初回 UI 言語** — `Accept-Language` から推定（Cookie 未設定時）
4. **フェーズ** — Community → Gallery / Shop / Works / Events

### コスト目安

- Google 無料枠: **月 50 万文字**
- ~100 MAU + 投稿時キャッシュ → 無料枠内で運用可能

### 関連エージェント

- `agents/business/Translation_Manager.agent.md`
- `agents/product/Community_Manager.agent.md`
- `agents/design/UI_UX_Designer.agent.md`（翻訳 Nexus UX）

---

*この更新により、Eldonia-Nexプラットフォームは真のグローバルサービスとして*
*アジア太平洋地域でのサービス展開が可能になります。*
